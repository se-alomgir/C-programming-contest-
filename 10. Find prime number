#include<stdio.h>

int main()
{
    int a,b ;

    printf("Enter a poitive number: ");
    scanf("%d", &a);

    if (a <= 1)
    {
        printf("%d is not a prime number.", a);
    }

    for (b = 2; b < n; b++)
    {
        if (a % b == 0)
        {
            printf("%d is not a prime number.", a);

            return 0;
        }
    }

    printf("%d is prime number.", a);

    return 0;
}
