#include <stdio.h>

int main() 
{
    float inches = 6, cm = 55;
    cm = inches * 2.54;
    printf("%.2f inches:%.2f centimeters\n", inches, cm);
    inches = cm / 2.54;
    printf("%.2f centimeters :%.2f inches\n", cm, inches);
    return 0;
}
